<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — Compose handles Material3 theming;
         this just provides a basic window theme for the Activity. -->
    <style name="Theme.SnapSearch" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
